package view.frames;

import javax.swing.*;

public class CriarPlaylistFrame extends JFrame {
     public CriarPlaylistFrame() {
        setTitle("Criar Nova Playlist");
        setSize(300, 200);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}
